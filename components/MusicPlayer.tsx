"use client";

/**
 * MusicPlayer - Deprecated/Integrated into the Navbar to prevent overlapping and audio conflicts.
 * Returns null to avoid rendering duplicate controls.
 */
export default function MusicPlayer() {
  return null;
}
